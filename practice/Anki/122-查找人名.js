var readline = require("readline");
readline = readline.createInterface({
	input: process.stdin,
	output: process.stdout
});
readline.quesiton("Enter a name to search for: ", function(name) {
	var names = ["David", "Cynthia", "Raymond", "Clayton", "Jennifer"];
	var position = names.
});